好，我们来专门设计一个 **“自动辅助线搜索（Automatic Auxiliary Construction Search）”** 子系统，保证：

* 与你现有的 **DSL + Fact–Theorem–Proof + Solver 架构** 完全兼容；
* 高度可扩展（可以像定理库一样配置新的“辅助线套路”）；
* 不是盲目暴力枚举，而是 **目标驱动 + 定理驱动 + 数值剪枝** 的搜索。

下面是纯设计、无代码。

---

# 1. 自动辅助线搜索要解决什么问题？

现实中做几何证明，很大一块难度在于：

> “该加哪条辅助线？”

这个模块的任务就是自动做这件事：

1. 在当前的 **几何构造 + Fact + 目标命题** 下，自动给出若干 **候选辅助构造**（中线、垂线、角平分线、对称点、外接圆/内切圆等）。
2. 评估它们是否有价值（能否让更多定理可用、让目标更接近）。
3. 以受控搜索的方式，把“好”的辅助线加入系统，再交给 **定理推理引擎** 去使用。

关键要求：

* 不能暴力枚举所有线，否则组合爆炸。
* 必须“目标导向 + 模式导向”。
* 必须与 **Theorem 库** 深度联动：为定理创造前提。
* 允许用 **数值模型** 来做剪枝。

---

# 2. 在整体架构中的位置

在你之前的项目结构基础上，增加一个模块：

```text
src/
  ...
  auxiliary_search/          # 自动辅助线搜索模块（新）
    aux_types.py             # 辅助构造类型/模板定义
    aux_patterns.py          # 辅助构造模式（预配置）
    candidate_generator.py   # 候选辅助线生成器
    evaluator.py             # 评估与打分（启发式+数值）
    strategy.py              # 搜索策略（迭代、限深等）
    applier.py               # 把选中的辅助构造应用到几何模型 & FactBase
```

交互关系：

* 输入：

  * 当前 `ProofState`（包含 FactBase、目标Goal、TheoremBase、NumericModel）
* 输出：

  * 一组 **AuxConstruction**（辅助构造）；
  * 或者返回“在预算内没找到有意义的辅助构造”。

Proof 引擎的工作流程会变为：

1. 尝试在当前构造下，用定理库证明目标。
2. 若长时间无进展或达不到目标：

   * 调用 `auxiliary_search` 生成若干辅助构造候选。
   * 选择得分高的辅助构造，应用到当前几何系统。
   * 更新 FactBase，再继续定理推理。
3. 在深度/数量限制内循环。

---

# 3. 核心概念：AuxConstruction（辅助构造）

**AuxConstruction** 是一个“可以加进图里的新构造步骤”，本质类似一条 DSL 语句，但由系统自动提议，而非用户输入。

典型种类：

* `Midpoint(M, A, B)` —— 作 AB 的中点 M
* `Perpendicular(L, P, AB)` —— 过 P 作 AB 的垂线 L
* `Parallel(L, P, AB)` —— 过 P 作 AB 的平行线 L
* `AngleBisector(L, A, B, C)` —— 作 ∠BAC 的角平分线
* `Reflect(P', P, L)` —— P 关于 L 的对称点 P'
* `Connect(A, B)` —— 补一条连接线段/直线 AB
* `Circumcircle(c, A, B, C)` —— 三角形 ABC 的外接圆
* `Incircle(c, A, B, C)` —— 三角形 ABC 的内切圆
* ……（可扩展）

一个 AuxConstruction 对象需要携带：

* `type`：辅助构造类型（例如 "midpoint"）
* `anchors`：依赖的已有点/线（如 A,B,P,AB 等）
* `predicted_theorem_patterns`：它“推荐”的用法（比如可能制造相似三角形/垂直/等腰）
* `score`：启发式评分（由 evaluator 决定）
* `cost`：搜索代价（用于搜索预算控制）

---

# 4. 辅助线搜索总体策略

可以理解为一个 **三层策略**：

1. **模式驱动（Pattern-driven）**：

   * 根据现有的 Fact 识别典型几何结构（如三角形、相邻角、圆、平行线），
   * 针对这些结构提议经典辅助构造（中线、高线、角平分线等）。

2. **目标驱动（Goal-driven）**：

   * 根据“要证明的结论类型”（平行、相等、相似、共圆等），
   * 选择有利于启用相关定理的辅助线，如：

     * 想证明角相等 → 考虑造平行线 / 圆周角。
     * 想证明比值相等 → 考虑造相似三角形的结构（角平分线、中线等）。

3. **定理驱动（Theorem-driven）**：

   * 从待用的定理出发，看哪些前提缺失，
   * 搜索“能产生这些前提的标准辅助线套路”。

搜索流程（概念上）：

* 给定当前 ProofState 和一个主 Goal：

  1. 收集所有适用的“辅助模式”（AuxPattern）
  2. 对每个 AuxPattern 生成有限个候选 AuxConstruction
  3. 用启发式 + 数值模型对候选打分
  4. 选 top-K 个尝试应用（可以 DFS/BFS/Best-First）
  5. 每应用一个：

     * 更新几何模型（新增对象/关系）
     * 在 FactBase 中加入新 Fact
     * 再调用 Theorem 引擎尝试证明/推进
  6. 若成功证明目标 → 收工；
     若达到搜索限度 → 报告“在当前限制下无法证明”。

---

# 5. “辅助线模式” AuxPattern 如何定义？

为了让“自动辅助线”像“定理库”一样可扩展，我们也要设计一个 **外部化的“辅助模式库”**，比如放在：

```text
data/aux_patterns/
  - triangles.yaml
  - circles.yaml
  - parallels.yaml
  ...
```

一条 AuxPattern 描述“在什么几何结构下，推荐做什么辅助构造”。

### 示例 1：三角形中点连线

**文字版：**

* 前提模式：

  * 存在三角形 ABC
* 推荐构造：

  * 作 `Midpoint(M, A, B)`
  * 作 `Midpoint(N, A, C)`
  * 作线 MN `Connect(M, N)`
* 关联定理：

  * `MN ∥ BC`
  * `MN = 1/2 BC`
  * 可用于相似三角形、平行角等

**结构字段（概念上）：**

* `id: TRIANGLE_MIDSEGMENT`
* `preconditions:`

  * `Triangle({A}, {B}, {C})`
* `constructions:`

  * `Midpoint({M}, {A}, {B})`
  * `Midpoint({N}, {A}, {C})`
  * `Connect({M}, {N})`
* `expected_new_facts:`（可选）

  * `Parallel(line({M}{N}), line({B}{C}))`
  * `EqualRatio(Segment({M}{N}), Segment({B}{C}), 1/2)`

---

### 示例 2：平行目标的角平分线

当目标是证明某角等于另一个角，或某两线平行。
可有一个 AuxPattern：

* 如果有三角形 ABC，且目标涉及角 ∠ABC = ∠CBD 之类，
* 推荐作角平分线 BD 或 CE 等。

这个模式文件将描述：

* 前提：`Triangle({A},{B},{C})` 且 Goal 中包含 `{B}` 顶点的角
* 构造：`AngleBisector({L}, {A}, {B}, {C})`
* 期望：产生等角、相似三角形等。

---

## 总结：AuxPattern = “条件 + 构造脚本 + 目的提示”

* **条件（preconditions）**：用 FactPattern 表达
* **构造脚本（constructions）**：用标准 AuxConstruction 语法
* **用途提示（expected_new_facts / tags）**：告诉评估器“这个构造一般用于启用哪些类型的定理（相似、共圆、平行等）”。

这样：

* 新增辅助套路只需要写配置文件。
* 核心搜索引擎无须改动。

---

# 6. 候选生成：candidate_generator 的逻辑

在 `candidate_generator` 模块中，核心逻辑：

1. 对当前 FactBase 做 **模式识别**：

   * 找所有三角形：任意三点不共线即可
   * 找所有具有已知等边/等角/垂直/平行的结构
   * 找所有涉及目标点/线/角的局部结构

2. 对每个识别出的结构，遍历所有 AuxPattern，看哪些 pattern 的 preconditions 能匹配上。

3. 对匹配上的 AuxPattern：

   * 将 pattern 中的占位符（如 {A},{B},{C}）绑定到实际点，
   * 生成具体的 AuxConstruction 序列。

4. 过滤掉明显重复或已存在的构造，例如：

   * 已有中点 M 了，就不要再提同一个中点
   * 已有过 P 的 AB 的垂线，就不要再造一次

结果是一组候选 AuxConstruction（单步 or 多步构造）。

---

# 7. 候选评估与打分：evaluator

`evaluator` 根据多个维度为候选 AuxConstruction 打分，主要考虑：

1. **目标相关性**

   * 辅助构造中涉及的点/线是否与目标命题中的点/线高度重合。
   * 例如目标证明 ∠A = ∠B，则涉及 A、B、这些点的构造优先。

2. **定理可用性提升**

   * 在引入 AuxConstruction 后，哪些定理的前提可能被满足？
   * 可以通过“模拟插入”的方式：

     * 临时加入构造产生的 Fact
     * 让 Theorem Matcher 计算当前“可匹配但前提尚不全的定理”中，有多少个前提因此被补上。
   * 这可以给出一个“定理解锁度”的评分。

3. **数值合理性**（使用 NumericModel）

   * 在当前数值模型中，试探性地“虚构”这个构造，检查：

     * 预期新 Fact 是否在数值上大致成立？

       * 比如做中点后，检测两个边是否长度近似相等。
       * 作角平分线后，检测两个夹角是否近似相等。
   * 若数值上严重不合理（误差大），削减评分甚至丢弃。

4. **复杂度与成本**

   * 构造步骤数量（一个 pattern 可能包含多步）
   * 引入的新点/线数量
   * 影响后续搜索空间大小

最后形成一个 `score = f(目标相关性, 可解锁定理数量, 数值合理性, 成本)`。

---

# 8. 搜索策略：strategy

`strategy` 决定如何在 Proof 过程中调用辅助线搜索，以及如何遍历多个候选。

典型策略设计：

1. **触发条件**

   * 定理推理引擎在当前构造下尝试证明了一定步骤/深度后仍无进展时，触发辅助线搜索。
   * 或者一开始就允许有限度地使用辅助线（例如最多增加 N 条）。

2. **搜索模式**

   * **迭代加深**：先试 1 条辅助构造；不行再增加到 2 条；…
   * **Best-First**：每次选评分最高的候选 AuxConstruction 尝试应用。
   * 为避免组合爆炸，可限制：

     * 最大辅助构造数
     * 每层尝试的 top-K 候选
     * 最大总搜索节点数

3. **回溯与撤销**

   * 某条辅助构造尝试后仍无法证明目标，可以撤销该构造（逻辑回溯），尝试下一个。
   * 为实现方便，可以在 ProofState 上做“复制-试验”的无副作用模式；只有在确定某构造有效时才合并入主状态。

---

# 9. 应用辅助构造：applier

`applier` 的职责：

* 将 AuxConstruction 变成真实的：

  * 几何对象（Point/Line/Circle 等）
  * Fact（例如 Midpoint、On 等）
* 逻辑上相当于：

  * 自动在 DSL 中插入这些构造句子
  * 然后重新跑 `semantic_builder` 的相关部分，更新 GeometryModel + FactBase

**注意：**

辅助构造本身也是一个“证明的一部分”，所以要在 Proof 结果中记录：

* 哪一步引入了哪些辅助构造
* 后续所有定理使用都可以追踪到这些构造产生的 Fact

---

# 10. 一个完整的“自动辅助线搜索 + 证明”示例流程（概念）

假设要证明：

> 在三角形 ABC 中，已知 AB = AC，证明 ∠ABC = ∠BCA。
> （虽然这个例子无需辅助线，但用来说明流程）

1. DSL 定义三角形 ABC + AB=AC；prove 角相等。
2. FactBase 拥有：

   * `EqualSegment(AB, AC)`
   * `Triangle(A,B,C)`
3. Theorem 引擎发现：

   * 定理：等腰三角形底角相等
   * 前提：`EqualSegment({P1}{P2}, {P1}{P3}) && NotCollinear(...)`
   * 可直接匹配→证明成功；不需要辅助线。

换一个更需要辅助线的常见情况：

> 已知：AB = AC，点 D 在 BC 上。
> 证明：AD 是角平分线（∠BAD = ∠DAC）。

1. 初始 Fact：

   * `EqualSegment(AB, AC)`
   * `On(D, line(B,C))`
   * `Triangle(A,B,C)`
2. 定理库中可能有“角平分线性质”的反向使用方式，但前提不够。
3. 证明引擎推进失败一段时间后，触发辅助线搜索。

**辅助线搜索模块动作：**

1. 发现：有等腰三角形 ABC；有点 D 在线 BC 上。

2. 根据 AuxPattern：

   * 某个 pattern 提议作对称点 E：P’ 为 C 关于 AD 的对称点，或作中垂线等；
   * 或者构造 `Midpoint(M, B, C)`, `Connect(A,M)`；

3. evaluator 检查数值模型 + 定理可用性：

   * 可能发现作 M 为中点后，`AM` 与 `AB`、`AC` 等形成很多相似结构，可解锁“中线定理”、“重心”、“相似三角形”等；
   * 比较不同候选的“解锁能力”，挑一个评分较高的。

4. 选中 `Midpoint(M, B, C)` → `Connect(A, M)` 作为辅助构造：

   * applier 加入新的点 M 和线 AM，更新 FactBase：

     * `Midpoint(M, B, C)`
     * `On(M, BC)`
     * `On(A, AM)` / `On(M, AM)`

5. Theorem 引擎重新工作：

   * 匹配等腰三角形的“中线 = 角平分线 = 高”的性质等定理。
   * 可能发现对应定理，并推出 `AD` 或 `AM` 的角平分线性质，从而接近目标，甚至直接完成证明。

（真实的最优辅助线可能不同，这只是示意“引入中点 + 连接”的作用）

---

# 11. 小结

**自动辅助线搜索模块** 的总体设计要点：

1. 有一个独立的 `auxiliary_search/` 模块，

   * 内部包括 **候选生成**、**模式配置**、**评估打分**、**搜索策略**、**应用器** 等子模块。

2. 核心抽象：

   * **AuxPattern（辅助模式）**：条件 + 构造脚本 + 预期用途
   * **AuxConstruction（具体辅助构造实例）**：某个模式在当前几何场景下的具体化
   * 可像定理库一样用 YAML/JSON 配置，以便无限扩展新套路。

3. 搜索方法：

   * 目标驱动 + 模式驱动 + 定理驱动
   * 结合数值模型进行剪枝和打分
   * 用有限深度/预算的搜索策略（Best-First / 迭代加深）

4. 与 Proof 引擎的关系：

   * Proof 引擎在“卡住”时调用辅助线搜索
   * 辅助线搜索提供一个或若干高价值构造
   * 应用后更新 FactBase & GeometryModel，再继续定理推理
   * 最终在 Proof 中记录“用到了哪些辅助线”。

---

如果你愿意，下一步可以继续细化其中任何一块，比如：

* **AuxPattern 文件格式的完整规范**（字段、语法、例子）。
* **辅助线搜索的搜索树结构与状态转移图**。
* **如何把定理前提缺失 → 反推可能的辅助构造类型**（“定理反向驱动”算法）。

你可以直接说“展开 AuxPattern 规范”或者“展开搜索树策略”等等。

